{
  "name": "@justinmchase/type",
  "version": "0.2.4",
  "exports": "./mod.ts",
  "imports": { "@std/assert": "jsr:@std/assert@^1.0.1" },
  "tasks": {
    "check": "deno fmt && deno lint && deno test",
    "coverage": "rm -rf cov_profile && deno test --coverage=cov_profile && deno coverage cov_profile"
  }
}
